using System;

class elements {
	static void Main() {
		
		string[] elements = new string[] {"fire", "earth", "water"};
		
		foreach (string element in elements) {
			Console.WriteLine(element);
		}
		
		Console.ReadLine();
	}
}
